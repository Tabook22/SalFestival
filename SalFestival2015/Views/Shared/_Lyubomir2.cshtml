@model IEnumerable<IramOman.Areas.Admin.ViewModels.folders>
<div class="modal-body">
    <div class="alert alert-warning">
        <span class="glyphicon glyphicon-warning-sign"></span>
        هل تريد حذف المجلد؟. سيتم حذف كافة المحتويات.
    </div>
    @*@using (Html.BeginForm("Lyubomir2", "ImageUpload", FormMethod.Post, new {id="my-form"}))
    {*@
        <div class="row">
            &nbsp;
        </div>
        <div class="row lert alert alert-warning">
            <div class="form-group ">
                <table class="table table-striped table-condensed table-bordered">
                    <tr>
                        <th>
                            folder name
                        </th>
                        <th>Delete</th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <input type="checkbox" name="deleteInputs" value="@item.foldername " />
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.foldername)
                            </td>

                            <td>
                                @*<a href="@Url.Action("Lyubomir2", new { id = item.foldername })" class="deletebutton">Delete</a>*@
                                @Html.ActionLink("حذف", "Lyubomir2", new { deleteInputs = item.foldername })
                                @*<a href='@Url.Action("/Lyubomir2/",new { id = item.foldername })' onclick="$.post(this.href); return false;">Delete</a>*@
                                @*@Html.ActionLink("حذف", "Lyubomir2", new { id = item.foldername })*@
                            </td>
                        </tr>
                    }
                </table>
                <!-- Section for buttons -->
                <div class="actions">
                    <a href="javascript:(function(){document.tableForm.submit();return void(0);})()">
                        Delete selected books
                    </a>
                </div>
            </div>
        </div>
        <div class="col-md-4 col-md-offset-4">
            <button type="button" class="btn btn-default"
                    data-dismiss="modal">
                Cancel
            </button>
            <button type="submit" id="approve-btn"
                    class="btn btn-danger">
                Save
            </button>
        </div>
    @*}*@

   <script type="text/javascript">
    $(function () {
        $('#approve-btn').click(function () {
            $('#modal-container2').modal('hide');
        });

    });


      </script>
   @* <script src="~/Scripts/jquery-2.1.3.min.js"></script>

    <script>
        $(function () {
            console.log("الحمد لله رب العالمين");
        $("#my-form").on("submit", function(e){
            e.preventDefault();
            console.log("الحمد لله رب العالمين");
        $.ajax({     type: 'POST',
            url: "/ImageUpload/Lyubomir2",
            data: $("form").serialize() + '&id=123' ,
            success: function () { alert("Successful"); },
            dataType: "json"
        });
    });
    });

    </script>*@
   @* <script type="text/javascript">
    $(function () {
        $(".deleteButton").click(function () {
            if (confirm('Are you sure you wish to delete this item?')) {
                $.post($(this).attr("href"));
            }
        });
    });</script>*@
